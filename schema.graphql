schema {
  query: Root
}

type Root {
  bookmarks(tag:  ID): [Bookmark]
  tags: [Tag]
}

type Bookmark {
  id: ID
  title: String
  url: String
  owner: User
  tags: [Tag]
}

type User {
  id: ID,
  first_name: String
}

type Tag {
  id: ID,
  name: String
}
