schema {
  query: Root
}

type Root {
  bookmarks: [Bookmark]
}

type Bookmark {
  id: ID
  title: String
  url: String
  owner: User
  tags: [Tag]
}

type User {
  id: ID,
  first_name: String
}

type Tag {
  id: ID,
  name: String
}
